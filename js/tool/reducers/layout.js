/**
 * Created by sinires on 17.08.17.
 */
const initialState = {
    isOpen: true
};

export default function layout(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

